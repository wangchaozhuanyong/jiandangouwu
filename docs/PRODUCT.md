# CloudBridge 产品范围与能力状态

## 产品定位

CloudBridge 是一个中英文 AI 服务商品展示与人工联系下单平台。客户端用于浏览、筛选、搜索、查看价格和提交单一联系方式；管理后台用于管理商品、分类、币种、人工订单与安全审计。

客户端与管理后台是独立应用。客户端不提供商城账户、登录、购物车或数量选择器。本地 MySQL 主平台后台使用邮箱与密码登录并支持可选 TOTP；当前 Sites 生产后台改由 ChatGPT 身份授权进入，客户端不会因此要求登录。

## 能力状态定义

- **当前已实现**：主平台代码和本地依赖中存在可运行、可测试的真实逻辑或持久化。
- **当前模拟**：遗留原型或设计页使用静态数据、浏览器存储或前端延时，不产生服务端结果。
- **未来规划**：目标架构或生产能力，当前代码和环境不保证存在。

文案、验收和交付报告必须使用以上状态，不得用“页面能点”替代真实业务证据。

## 当前 Sites 生产状态

- 公开站点已发布到 `https://cloudbridge-wangchao-ai-market.syrupy-hill-4912.chatgpt.site`，使用 Sites Worker、D1、R2 和 ChatGPT 管理员身份授权；尚未绑定自有域名。
- 客户端可以公开浏览商品、分类、双语内容和币种报价，管理后台可以维护当前 Sites 数据。真实客服渠道尚未填写，`acceptOrders` 与客服开关保持关闭，因此当前是可公开浏览、不可真实接单的安全状态。服务端要求先完成并启用至少一个真实渠道，再显示客服入口，最后才能开启接单；占位账号或错误跳转地址不能绕过这项门禁。
- 管理工作台会实时查询全部在售商品，并使用与前台一致的固定规则区分库存数据冲突、售罄和 1–3 件低库存；返回全库计数和前六项优先样本。该结果是当前数据库快照，不是通知投递、持久化告警历史或库存流水。
- Sites 媒体库可以上传 PNG、JPEG、WebP 到 R2，列出已登记对象与数据库引用，并把商品和首页轮播的现有图片引用批量迁移到唯一的新地址。旧文件不会在替换时自动删除；只有服务端重新核对为 0 引用的 R2 文件才允许删除。打包静态图片不可从后台删除，备份对象也不通过公开 `/media/` 路由提供。
- 已创建生产加密恢复点并通过 R2 回读、SHA-256、解密和记录数校验。恢复流程支持把真实生产快照重新封装给一次性公钥，在本机一次性内存 SQLite 中导入全部 15 张业务表、回读记录并检查外键，再把签名结果写回备份审计；也可生成只允许导入新 D1 的受保护 SQL、验证查询、校验清单和回滚说明。当前 D1 不会被这些操作修改；新 D1 的真实远程导入、管理员核验、切换与回滚演练仍未完成。
- 390px 与桌面生产回归确认首页、商品详情和后台无控制间重叠；移动端非详情页的中转入口位于正文之后、页脚之前，商品详情页入口保持在固定下单栏上方。中转站 URL 尚未配置。

## 当前能力矩阵

| 能力 | 当前状态 | 当前证据与边界 |
| --- | --- | --- |
| 商品展示、双语切换、币种切换 | 当前已实现 | Next.js 客户端从 NestJS/MySQL 读取目录与汇率；种子汇率不等于实时市场数据 |
| 商品名称模糊搜索、单选分类筛选 | 当前已实现 | API 按当前语言在 MySQL 中执行受控模糊匹配，可组合分类 |
| 分类排序、显示状态、商品归类 | 当前已实现 | 管理后台通过 RBAC API 写入 MySQL，并使用版本号防止覆盖并发修改 |
| 联系方式下单与订单凭证 | 当前已实现 | API 校验服务器价格/库存/政策和真实渠道就绪状态，联系方式加密，幂等创建 `MANUAL_PENDING` 订单；不代表已付款 |
| 有限库存预留到期返库 | 当前已实现/请求驱动 | MySQL 与 Sites 都会在商品、下单、工作台或订单访问时取消到期的 `MANUAL_PENDING` 有限库存预留并只返库一次；MySQL 历史订单不推断旧库存，当前没有 Cron、队列或持久化任务运行历史 |
| WhatsApp、Email、Telegram、QQ、微信联系配置 | 当前已实现/未来规划 | 联系渠道从数据库读取；占位账号不能启用，各直连渠道必须使用批准协议；真实客服响应和外部送达仍需运营证据 |
| 在线支付完整状态 | 当前模拟 | 仅供设计评审；支付关闭时客户端不会进入支付步骤，也不包含银行卡输入框 |
| 管理后台商品、分类、订单、汇率 | 当前已实现 | 本地独立管理应用与受权限保护 API；订单支持真实筛选分页、详情、负责人、合法状态流转和时间线；尚未部署 AWS |
| 管理工作台真实总览与告警边界 | 当前已实现/自动告警未来规划 | `/admin/dashboard` 读取商品、分类、未结束订单、最近订单和全库实时库存风险，并在读取前运行请求驱动的到期预留核对；通知投递事件、持久化库存告警历史和自动安全告警仍明确显示未采集/未开发，不显示演示数字 |
| 人工售后订单视图 | 当前已实现 | `/admin/disputes` 真实读取 `REFUND_PENDING`、`REFUNDED`、`DISPUTED` 订单并复用订单权限、状态机和审计；已完成桌面、390px、320px 与中英文验收，但它不是独立退款或争议案件系统 |
| 人工收款记录 | 当前已实现 | `/admin/payments` 只读投影订单状态历史中的 `PAID`、`REFUND_PENDING`、`REFUNDED`、`DISPUTED` 人工事件；全部标记外部资金动作未核验，已完成桌面、390px、320px 与中英文验收 |
| 对账准备中心 | 当前已实现/外部对账未来规划 | `/admin/reconciliation` 按 `orders.read` 读取全部人工收款事件，展示内部记录类型、币种分布、最近记录和上线门禁；外部账单、交易 ID、实际到账、匹配与差异统一标记未采集或未开发 |
| Telegram 新订单通知准备 | 当前已实现/外部发送未来规划 | `/admin/telegram-bot` 已真实保存非密钥配置并生成服务端固定虚构订单脱敏预览；服务端始终返回未连接、未有效启用、未配置 Token、未尝试投递和未外部核验，不请求 Telegram |
| 通知就绪中心 | 当前已实现/真实投递未来规划 | `/admin/notifications` 按 `settings.read` 只读现有 Telegram 配置，真实展示 `NOT_CONNECTED`、Token 未配置、外部未核验、上线门禁和“投递证据未采集”；没有发送、未读、失败、重试或投递记录能力 |
| 双语内容工作台 | 当前已实现 | `/admin/translations` 按账号权限聚合 MySQL 中的商品、分类、首页轮播、客服渠道和站点设置；保存复用所属领域接口与权限，不跨语言回退 |
| 媒体库与引用管理 | 当前已实现/部分运行时受限 | Sites `/admin/media` 读取 `media_objects` 与 R2 状态，支持 5 MB 内 PNG/JPEG/WebP 上传、唯一地址替换引用、公开路径复制及 0 引用安全删除；上传、替换、删除与原因均写审计。MySQL 本地运行时仍只聚合商品和轮播引用，不拥有第二套对象存储 |
| 安全审计信号 | 当前已实现/自动检测未来规划 | `/admin/security-events` 只读投影最近 100 条 MySQL 审计记录中的登录、授权、敏感数据和关键配置事件；固定复核优先级不是威胁检测、SIEM 或自动告警结论 |
| 数据安全就绪中心 | 当前已实现/数据治理未来规划 | `/admin/data-security` 只读展示当前代码保护边界、管理员会话和有权读取的最近 MySQL 审计样本；不等于合规认证、已批准保留期限、删除完成、不可变存储或生产密钥管理 |
| 备份与恢复 | 当前已实现/生产切换未来规划 | Sites 每天首次访问自动把 D1 业务表加密写入 R2，后台可创建、下载、重新校验和逻辑验证；恢复运行器把真实快照恢复到一次性内存 SQLite 并回传签名证据，也可生成经过二次本地导入验证的新 D1 候选 SQL、校验清单与回滚说明。异常监测要求最近备份、今日自动备份、无失败、30 天内隔离 SQLite 演练和外部告警送达全部有证据；当前外部告警未连接，因此不会显示完全就绪。候选包不等于新 D1 已创建、远程导入、切换或回滚 |
| 系统集成就绪中心 | 当前已实现/外部集成与后台任务未来规划 | MySQL `/admin/integrations` 读取本次 API/MySQL/Valkey 健康探测，以及当前账号有权读取的币种和 Telegram 配置；Sites 对应页面另外核验 D1、R2 与 ChatGPT 身份边界。邮件、支付、外部汇率、后台任务和 AWS 部署均不伪装为已接通或已运行 |
| 管理员登录、MFA、会话、角色、权限和审计 | 当前已实现/部分管理未来规划 | 密码主登录、可选 TOTP、安全 Cookie、CSRF、RBAC、MySQL 审计、当前账号 Valkey 会话查看/撤销、成员角色/角色权限读写、非系统角色创建/资料编辑/空角色删除，以及其他成员的启用、停用、解锁和 TOTP 重置已实现；邀请与跨账号会话明细/单个处置仍未实现 |
| 支付提供商、资金流水、外部结算与 Webhook | 未来规划 | 当前没有提供商连接、外部交易 ID、账单、实际到账、Webhook、自动匹配或差异处理；对账准备中心只展示内部证据范围和缺失门禁 |
| 数据分类政策、保留删除、运行备份恢复和机密管理 | 未来规划 | 就绪中心会明确显示缺失门禁；当前没有批准政策、自动到期、删除/匿名化、隐私请求、已部署的 AWS 备份恢复、密钥轮换或生产环境证据 |
| AWS staging | 未来规划 | CDK 模板已通过 synth，但未创建任何计费资源 |

## 客户端规则

- 首页保持首屏品牌轮播和商品陈列；搜索和分类只属于商品陈列区。
- 搜索对当前语言及官方产品名称做不区分大小写的模糊匹配，并可与一个分类组合筛选。
- 分类由后台管理中英文名称、启停、顺序和商品归属；稳定 ID 不因改名或排序改变。
- 语言与币种独立切换。中文界面只出现中文界面文案，英文界面只出现英文界面文案；官方专名、产品名、币种代码和机器标识保留原值。
- 下单只收集一种联系渠道及对应值，不增加账户、购物车或数量。
- 在线支付关闭时，只能生成“待人工确认”订单，不显示支付步骤。
- 不提供公开订单查询。客户保留订单凭证并通过客服处理，避免订单号与联系方式枚举。
- 正式隐私、服务、交付、退款及 Cookie 页面必须保持独立，设计稿上线前仍需业务与法律审核。
- 商品售罄、低库存、加载、无结果和错误状态必须有明确反馈，不能只靠颜色表达。
- 页头与页脚品牌区复用同一 CloudBridge Logo 和横向结构，只允许针对所在容器调整尺寸；图片不得拉伸，品牌主名称下不显示副标题。

## 管理后台规则

- 后台负责商品、分类、双语内容、价格、库存、币种、客服渠道和订单工作流。
- `/admin/categories` 读取真实 MySQL 分类并把写入入口严格绑定 `catalog.write`；只有 `catalog.read` 的账号仍可查看分类和影响概览，但不能看到新增或编辑操作。
- 分类影响概览只统计当前分类接口返回的非归档分类、商品关联、顺序和双语完整度。非启用分类会退出公开筛选导航，但不会自动隐藏、移动或删除关联商品；归档分类也会退出当前管理列表。
- `/admin/products` 读取真实数据库商品并把新增、编辑与保存入口严格绑定 `catalog.write`；只有 `catalog.read` 的账号仍可使用服务端 30 条分页、双语名称或 slug 搜索、状态筛选、前后页和 URL 恢复，但不能看到写操作。默认目录排除归档商品，显式选择“已归档”后仍可查看。
- 商品页影响概览只统计当前服务端筛选结果的当前页并与单独加载的非归档分类列表交叉检查；页面同时显示当前筛选总数，但不把本页指标宣称为全库聚合。公开列表只读取 `ACTIVE` 商品；前台现有规则把有限库存 0 显示为售罄、1–3 显示低库存。工作台另有服务端全库实时库存风险摘要，但两者都不是库存流水、通知、持久化告警历史或发布记录。
- `/admin/dashboard` 的四项主指标与最近订单来自现有 `GET /v1/admin/overview`。最近订单最多六条，只是样本而不是订单总数；未启用商品只能由商品总数减去启用数确定性派生。
- 工作台把 MySQL 到期预留标记为 `IMPLEMENTED_REQUEST_DRIVEN`：商品、下单、工作台或订单访问会先核对最多 100 条候选，不代表存在 Cron、队列或无人值守运行历史。库存风险标记为 `IMPLEMENTED_LIVE_QUERY`，只证明本次请求查询了全部 `ACTIVE` 商品并返回风险计数和前六项样本；通知失败数量和自动安全告警没有证据时仍必须显示 `NOT_COLLECTED` 或 `NOT_IMPLEMENTED`。
- 订单中心的列表、详情、负责人和状态时间线读取真实 MySQL 数据；客户端只能选择服务端返回的合法下一状态，状态、历史和审计在同一事务内提交，并以旧状态和更新时间防止并发覆盖。
- 订单的付款、退款和争议状态只表示管理员人工记录。没有外部支付、退款或争议证据时，不得将这些状态解释为资金动作已经自动完成。
- `/admin/disputes` 的实施目标是从同一订单数据中集中展示 `REFUND_PENDING`、`REFUNDED`、`DISPUTED`，并复用 `orders.read`、`orders.write`、订单状态机、CAS 并发控制和事务审计；不得另造一套前端售后状态。
- 人工售后订单视图不提供独立退款申请金额、部分退款、证据、双人审批、支付商回执或资金结算能力；需要这些能力时必须单独确认领域模型、安全规则和数据库变更。
- `/admin/payments` 只读展示 `OrderStatusHistory.toStatus` 为 `PAID`、`REFUND_PENDING`、`REFUNDED`、`DISPUTED` 的人工事件；读取要求 `orders.read`，打开订单详情继续沿用原订单权限，收款页不得修改订单或事件。
- 人工收款记录不是支付流水或会计台账，每条事件必须显示 `externalActionVerified: false`；没有实际到账、部分或多次付款/退款、手续费、税费、支付方式、外部 ID、凭证、结算批次、对账或跨币种汇总能力。
- `/admin/reconciliation` 是 `orders.read` 保护的只读准备状态：读取全部人工收款事件，只按原始币种统计记录数并最多展示八条最近内部记录；没有读取权限时不得请求接口。
- 对账准备中心中的 `NOT_COLLECTED` 表示外部证据不存在于当前系统，不等于金额为 0；`NOT_IMPLEMENTED` 表示对应支付连接、Webhook 或匹配流程尚未开发。人工状态历史不能解释为渠道账单、银行流水、实际到账、结算结果或差异证据。
- `/admin/media` 在本地 MySQL 运行时仍是现有商品与首页轮播记录的只读聚合；商品使用 `catalog.read`，首页轮播使用 `content.read`，缺少其中一种权限时只显示获准范围。该运行时没有对象存储写入。
- Sites `/admin/media` 另外读取 D1 `media_objects` 和对应 R2 对象状态。上传要求 `catalog.write` 或 `content.write`，只接受文件签名与声明类型相符、大小不超过 5 MB 的 PNG、JPEG、WebP，并生成不可复用的版本化公开地址。
- 批量替换可能同时影响商品和首页轮播，因此要求 `catalog.write` 与 `content.write` 两项权限、8–500 字符原因和显式确认；数据库只更新仍精确引用旧路径的记录，旧 R2 文件保持不动。管理员确认替换结果后可单独处理变成未引用的旧文件。
- 删除只适用于 `media_objects` 登记的 R2 上传文件；服务端删除前重新统计商品和轮播引用，非 0 时返回冲突。删除要求写权限、原因与二次确认并写审计；打包 `/assets/` 文件不能通过后台删除。
- 公开 `/media/` 路由只接受受控 `uploads/YYYY/MM/` 对象键并返回不可变缓存，备份、路径穿越、SVG 和未登记格式不能借该路由公开。当前能力不等于图片病毒扫描、自动压缩、尺寸重采样、内容审核、CDN 指标或跨区域副本。
- `/admin/team` 真实读取管理员的安全白名单字段与角色；角色分配要求 `team.manage`、最近认证、业务原因、二次确认、CAS 与事务审计。管理员不能修改自己的角色，最后一名正常超级管理员不能被降权。
- `/admin/roles` 真实读取权限矩阵；创建非系统角色、编辑双语名称与说明、修改权限和删除空角色都要求 `roles.manage`、最近认证、业务原因、二次确认与事务审计，资料修改、权限修改和删除还使用 `updatedAt` CAS。角色键创建后不可修改，角色至少保留一个权限，仍有关联成员的角色不能删除，`SUPER_ADMIN` 的资料、权限和生命周期全部由系统保护。
- `/admin/translations` 只聚合当前账号有权读取的五类双语内容；商品/分类、轮播、客服渠道和站点设置分别沿用 `catalog`、`content`、`support`、`settings` 的读写权限与所属 API，不提供跨模块通用写权限。
- 双语内容保存要求中文和英文都非空并服从所属字段长度；不跨语言回退。站点设置继续要求业务原因和版本 CAS，其他实体继续沿用各自版本 CAS；并发或权限拒绝不能伪装成保存成功。
- `/admin/logs` 读取受 `audit.read` 保护的真实审计 API，使用服务端关键词、结果、操作来源、目标类型和时间筛选，并以 30 条一页浏览完整匹配历史；筛选、页码和返回导航状态保存在 URL，目标类型选项来自服务器分面。
- `/admin/logs` 可按当前已应用的服务器筛选安全导出 CSV；导出要求 `audit.read`、CSRF、8–500 字符业务原因、精确确认值与客户端强确认，单次上限 5,000 条，超限拒绝并要求缩小筛选。MySQL 额外强制五分钟内最近认证；Sites 使用 ChatGPT 平台每次请求提供的托管身份，CloudBridge 不声明无法读取的密码/TOTP 最近认证时间。
- 审计列表与 CSV 只包含事件 ID、追踪 ID、时间、动作、结果、操作者姓名/邮箱、目标类型/ID 和原因；数据库中的前后快照、IP 哈希、操作者内部 ID 与整行数据不得进入响应或下载。CSV 使用 UTF-8 BOM、标准引号转义和表格公式注入防护；生成成功、最近认证拒绝或数量上限拒绝写审计，格式校验错误不伪装为已生成事件。当前仍没有正式保留策略、不可变归档或差异查看。
- `/admin/security-events` 复用 `audit.read` 与现有审计 API，只挑选登录认证、角色权限、敏感联系方式、关键设置以及全部拒绝记录；普通成功的商品或内容编辑不进入安全信号。
- 安全信号的“优先复核/常规复核/信息记录”是前端固定规则，不是机器学习、关联检测或外部威胁情报结果。页面不提供自动告警、自动账号锁定、跨账号会话处置、事件指派、结案或导出；当前账号的其他 Valkey 会话可在安全中心手动撤销。
- `/admin/data-security` 是只读就绪中心：无论是否拥有 `audit.read` 都可查看非敏感代码边界和当前会话安全投影；只有获得 `audit.read` 时才请求最近 100 条审计记录，没有权限时不得发起该请求。
- 数据安全页面中的 `IMPLEMENTED_CODE` 只证明仓库存在相应代码与自动测试，`NOT_DEFINED`、`NOT_IMPLEMENTED`、`NOT_CONNECTED` 分别表示尚无批准规则、尚未开发或尚未连接。页面不创建保留、删除、匿名化、隐私请求、KMS、备份或合规结果。
- `/admin/secrets` 是无值只读就绪中心：只投影 `.env.example`、API 使用代码和 AWS CDK 中能够核对的配置名、来源、消费边界与门禁，不读取本地 `.env` 或 Secrets Manager，也不显示密钥值、后缀、版本、创建时间或轮换成功。
- CDK 当前定义 RDS 生成凭据、Valkey、管理员会话和应用加密四个机密来源，以及只注入 API ECS 任务的六个绑定；这只属于 `DEFINED_INFRA` 代码证据。AWS 尚未部署，运行时元数据接口、自动轮换、双版本回滚、客户管理 KMS Key 和泄露处置手册尚未具备。
- `AUTH_ENCRYPTION_KEY` 当前同时保护订单联系方式与 TOTP 密钥，数据域尚未分离；联系方式检索使用普通 SHA-256 规范化哈希，不是 HMAC。页面不得用伪造的 Stripe 密钥、后缀或轮换天数掩盖这些缺口。
- `/admin/backups` 在 Sites 运行时读取真实 D1 备份清单，`settings.write` 可创建、重新校验和验证恢复包，`settings.read` 可查看和下载加密文件；接口不返回明文联系方式，下载响应禁止缓存。逻辑验证检查主键、表关联、配置 JSON 和订单联系方式能否用当前密钥解密。隔离演练工作台使用一次性 RSA 公钥重新封装快照，本地先在内存 SQLite 导入、回读并执行 `foreign_key_check`，完成结果必须带限时签名证明才能写回审计；本机还可选生成新 D1 导入候选目录。
- Sites 自动备份采用每日首次访问触发，同一天通过唯一调度键最多创建一次。快照在同一 D1 batch 中读取固定业务表，使用 `CLOUDBRIDGE_DATA_KEY` 的 AES-256-GCM 加密后写入 R2，并在记录为 `VERIFIED` 前完成回读、SHA-256、解密和逐表记录数校验。
- Sites 后台的重新校验、隔离 SQLite 演练和候选包生成都不修改当前业务表；候选目录包含明文业务元数据，固定使用 `0700/0600` 权限并拒绝覆盖。当前不提供生产一键恢复、自动删除备份、跨区域副本或不可变保留。正式覆盖恢复仍必须把候选导入真实的新 D1，验证 schema、逐表记录数、管理员访问、订单数据和 R2，再安排生产切换与回滚。
- Sites 备份异常监测只在管理页读取时计算并展示，不代表外部告警已经投递。邮件、短信或 Telegram 告警尚未连接，`EXTERNAL_ALERT_DELIVERY` 门禁固定失败，因此缺少真实送达证据时页面只能显示需要复核，不能显示完全就绪。
- 2026-07-29 的生产演练已把最新快照的 15 张表、77 条记录导入一次性内存 SQLite 并逐表回读，外键违规为 0；该证据已写回备份元数据和审计，但不扩大为独立 D1 切换或覆盖恢复成功。
- 本地 `cloudbridge_mysql` 和 `cloudbridge_redis` 命名卷只代表同机持久化，Valkey AOF 也不是离机备份；页面必须把它们标记为 `NOT_A_BACKUP`，不能显示最近成功时间、文件大小、校验和、保留期或可恢复结论。
- CDK 中 RDS 7 天自动备份、删除保护和 `RETAIN`，以及 Valkey 7 份快照和固定窗口只属于 `DEFINED_INFRA`；AWS 未部署时必须保持 `NOT_DEPLOYED`，RPO/RTO 与恢复完整性在真实演练前不得写成已达成。
- `/admin/integrations` 是真实只读集成边界页：公开健康接口只证明本次 API 请求、MySQL `SELECT 1` 与 Valkey `PING` 在各自 1500ms 上限内完成；`catalog.read` 才读取币种与已保存汇率，`settings.read` 才读取 Telegram 配置，无权限时不得发起相应请求。
- 集成页中的币种与汇率是管理员维护的 MySQL 记录，不是外部提供商同步；Telegram 保持 `NOT_CONNECTED`，Valkey 仅在本次健康请求中标记 `RUNTIME_VERIFIED`，本地 `/assets/` 不得写成对象存储，CDK 模板不得写成 AWS 已部署。
- 本地 MySQL 集成页当前已接通外部服务和已实现后台任务均为 0；Sites 已核验自身 D1/R2/Worker，但仍没有邮件、支付、外部汇率、调度器、队列、任务事件或统一集成可观测性。缺少这些能力时不得伪造成功率、重试数、计划时间、追踪编号或健康结论。
- 后台守卫在每次受保护请求重新读取 MySQL 中的账号状态与有效权限；账号被停用或权限被撤销后，旧 Valkey 会话不能继续依赖缓存权限访问。
- 完整联系方式默认不返回；具备独立权限的管理员还必须处于最近认证窗口、填写业务原因并确认，明文只在订单详情内短时显示。
- `/admin/telegram-bot` 使用 `SiteSetting` 键 `notifications.telegram.new-order` 真实保存 `requestedEnabled`、内部群名称、固定 `ORDER_CREATED` 事件和字段白名单；它不保存 Bot Token 或 Chat ID，群名称和业务原因也拒绝夹带疑似 Token 或 Chat ID。
- Telegram 配置读取要求 `settings.read`；保存要求 `settings.write`、五分钟内最近认证、业务原因、版本 CAS 和事务审计。请求启用不等于实际启用，服务端始终派生 `NOT_CONNECTED`、`effectiveEnabled: false`、`tokenConfigured: false`、`externalDeliveryVerified: false`。
- Telegram 持久化配置损坏时整体失败关闭；未来时间的最近认证时间戳被拒绝，版本号限制在安全整数范围。
- Telegram simulation 只使用服务端固定虚构订单生成最多 8 个白名单字段的脱敏预览，示例联系方式为 `de***@invalid.example`，始终 `deliveryAttempted: false`，不发送、不重试、不排队、不写发送记录。
- `/admin/notifications` 与 `/admin/telegram-bot` 复用同一只读配置缓存；通知中心只判断真实上线门槛，没有投递事件存储时必须显示“未采集 / Not collected”，不能显示未读数、失败数、发送结果或重试状态。
- 后台九个一级入口和 24 个页面均已进入真实或明确受限状态，独立设计预览路由已经删除。明确受限页面仍必须直接展示未开发、未连接、未部署或无权限状态，不能用模拟成功、按钮反馈或虚构记录代替实现证据。
- 每个后台页面只有一个当前页标题，由顶栏持有；内容区直接进入真实数据或任务控件。
- 任何“保存成功”“汇率已更新”“权限已生效”都必须对应 API 的真实结果；通知未接通时必须标记未连接。
- 后台使用邮箱与密码主登录；管理员可在安全中心开启或关闭 TOTP，默认关闭。管理员 Token 不写入 `localStorage` 或 `sessionStorage`。
- 会话使用 Valkey 与 `HttpOnly`、`Secure`、`SameSite=Strict` Cookie；API 同时执行 CSRF 和权限码校验。
- MySQL 运行面的安全中心只列出当前账号的服务端会话，支持单独撤销或保留当前会话并撤销其余会话；当前会话必须通过退出登录结束。列表不采集或推断设备名、IP、浏览器指纹，所有撤销结果写入 MySQL 审计。
- MySQL 团队页按 `team.manage` 展示成员账号状态、认证提供方、密码是否已配置、TOTP 状态、失败次数和锁定截止时间；启用、停用、解锁和其他成员 TOTP 重置要求最近认证、业务原因、确认、CAS、事务审计，并撤销该成员全部 Valkey 会话与 TOTP 流程。管理员不能从团队页操作自己。
- Sites 运行面的后台密码、双重验证和会话由 ChatGPT 身份层管理；CloudBridge 不枚举或伪造平台会话，也不显示不可执行的撤销按钮。
- 揭示联系方式、支付启停、退款、权限、导出和恢复操作必须设计拒绝、原因、重新认证、二次确认与审计状态。
- 未来真实接通 Telegram 时，目标是推送到用户确认的内部订单管理群；只允许订单号、商品、金额、币种、状态、创建时间、联系方式渠道和脱敏联系方式进入消息模板。
- Telegram 连接配置属于敏感操作，必须重新认证并记录审计；原型测试结果必须明确标记为模拟，不能写成已真实发送。
- 所有后台结构化数据表一条记录只占一行。订单号、订单时间、商品、金额与 USDT、联系渠道、脱敏联系账号、处理状态、付款状态、负责人和操作必须使用独立列；其他表中的名称、代码、工作组、时间、事件 ID 和追踪信息遵循同一规则。
- 窄屏不把数据表转换为多行卡片，也不隐藏关键列；表格在自己的容器内横向滑动，订单号与操作列可保持吸附，超长内容用省略号和完整说明兼顾扫描效率与可访问性。
- 后台侧栏、登录页和品牌预览与客户端复用同一 CloudBridge Logo；只调整容器尺寸并保持原始宽高比，不显示“云桥管理中心”“Operations console”或其他 Logo 副标题。

## 明确不做

- 当前阶段不接入真实支付、Telegram 发送、生产通知或生产数据。
- 未确认费用、域名和证书前不执行 AWS `deploy`。
- 不从其他项目复制业务模块、角色代码、路由、接口字段、数据库结构、端口或部署脚本。
- 不因未来规划提前引入消息队列，也不为本地 MySQL 路径复制第二套对象存储；Sites 媒体继续使用平台已经绑定的 R2。

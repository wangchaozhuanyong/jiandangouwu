# 工作台真实性与响应式验收

日期：2026-07-29

## 结论

- `/admin/dashboard` 已删除硬编码的“临期订单 2”“低库存 3”“通知失败 1”和旧设计流程弹窗。
- 商品、订单与分类指标继续来自现有 `GET /v1/admin/overview`；未启用商品、最近订单样本和最近订单时间只从同一响应确定性派生。
- 库存预留返库、低库存告警聚合、通知投递事件和自动安全告警分别显示 `NOT_IMPLEMENTED` 或 `NOT_COLLECTED`，不再把缺失证据显示为 0。
- 未新增 API、Prisma 模型、数据库迁移、权限码、依赖或环境变量。

## 浏览器验收

| 视口 | 语言 | 页面横向溢出 | H1 | 可见按钮小于 44px | 标题/语言重叠 |
| --- | --- | ---: | ---: | ---: | --- |
| 1280 × 900 | 英文 | 0px | 1 | 0 | 无 |
| 390 × 844 | 中文 | 0px | 1 | 0 | 无 |
| 320 × 844 | 英文 | 0px | 1 | 0 | 无 |

- 390px 和 320px 的三项真实数据范围改为单列；四项能力边界保持完整标题、说明、状态和 44px 以上任务入口。
- 320px 实际内容宽度为 305px，能力行左右边界为 13px / 292px，没有裁切。
- 最近订单表在桌面容器中为 `clientWidth=933px`、`scrollWidth=1860px`，继续只在自身容器横向滚动；页面级溢出为 0。
- 刷新按钮真实重读现有 GET；“查看商品”真实进入 `/admin/products`。
- 浏览器控制台 warning/error 为 0。
- 一次性本地 QA 管理员、角色与相关审计记录已退出并删除，复核计数为 `0 / 0 / 0`。

## 自动检查

- 根目录 `npm run check` 通过。
- 平台回归 45/45、管理后台 44/44、API 69/69、客户端 6/6 通过。
- 遗留原型与 Sites、五个工作区类型检查、Next.js/Vite/NestJS/CDK 生产构建全部通过。
- `git diff --check` 与 `npm run check:rules` 通过。

## 视觉证据

- `01-desktop-en.png`
- `02-mobile-390-zh-top.png`
- `03-mobile-390-zh-boundary.png`
- `04-mobile-320-en-top.png`
- `05-mobile-320-en-boundary.png`

浏览器长页面整页截图发生工具侧拼接异常，因此已删除异常文件并改用真实视口分段截图；该异常不作为页面缺陷或验收证据。

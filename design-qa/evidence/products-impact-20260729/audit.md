# 商品库存与上架概览及写权限验收

日期：2026-07-29

## 结论

- `/admin/products` 已删除旧“商品库存与发布”设计流程，改为基于现有商品与分类 GET 的真实只读概览。
- 当前已加载切片包含 8 个商品：8 个在售、6 个有限库存、2 个不限库存；按前台现有规则识别 1 个售罄和 1 个低库存提示。
- 概览明确限定为当前搜索结果第 1 页、最多 100 条；商品与非归档分类是两次独立读取，不冒充全库总量或事务快照。
- 一次性 `catalog.read` 账号没有新增、编辑或保存入口；商品图片补齐有意义的替代文本。服务端 `catalog.write` 继续是最终写权限边界。
- 本页不提供库存流水、库存预留返库、阈值配置、自动告警或发布记录；未新增 API、Prisma 模型、数据库迁移、权限码、依赖或环境变量。

## 浏览器验收

| 视口 | 语言 | 页面横向溢出 | H1 | 新增/编辑入口 | 可见按钮小于 44px | 标题/语言重叠 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 1280 × 900 | 英文 | 0px | 1 | 0 / 0 | 0 | 无 |
| 390 × 844 | 中文 | 0px | 1 | 0 / 0 | 0 | 无 |
| 320 × 844 | 英文 | 0px | 1 | 0 / 0 | 0 | 无 |

- 桌面商品网格为三列，8 张卡片图片替代文本均非空；概览弹窗为 900 × 782.2px，四项摘要和 8 条单行记录完整渲染。
- 桌面影响表 `clientWidth=860px`、`scrollWidth=1640px`，页面级溢出为 0。
- 390px 弹窗左右边界为 10px / 380px，`clientHeight=783px`、`scrollHeight=1080px`；表格 `clientWidth=327px`、`scrollWidth=1640px`。内部横向滚动实测 `scrollLeft=1300px`，页面级溢出仍为 0。
- 320px 正常视口中标题、概览入口、只读说明、搜索框和商品卡片完整可读；弹窗左右边界为 10px / 310px，`clientHeight=783px`、`scrollHeight=1181px`，表格 `clientWidth=257px`、`scrollWidth=1640px`，标题与关闭按钮重叠面积为 0。
- 搜索 `Codex` 后真实列表收敛为 1 个商品，概览同步显示 `Loaded products=1`、`Current search “Codex”` 和 1 行记录；清空后恢复 8 个商品。
- `Escape` 可关闭弹窗并把焦点返回概览入口；浏览器控制台 warning/error 为 0。
- 一次性 QA 管理员、角色与关联记录已退出并删除，复核计数为 `0 / 0 / 0`。

## 自动检查

- 商品影响模型 5 项测试通过，覆盖加载切片、前台库存边界、双语/库存冲突、分类状态和重复顺序。
- 管理后台 53 项测试和平台流程专项测试通过；TypeScript 检查与 `git diff --check` 通过。
- 根目录 `npm run check` 全量通过：平台回归 45/45、管理后台 53/53、API 69/69、客户端 6/6；遗留原型、Sites、五个工作区类型检查和全部生产构建通过。
- `git diff --check` 与项目规则检查通过。

## 视觉证据

- `01-desktop-en-readonly.png`
- `02-desktop-en-impact.png`
- `03-mobile-390-zh-readonly.png`
- `04-mobile-390-zh-impact-top.png`
- `05-mobile-390-zh-impact-table.png`
- `06-mobile-390-zh-impact-signals.png`
- `07-mobile-320-en-readonly.png`
- `08-mobile-320-en-impact.png`

320px 超长整页截图在工具预览中发生极端缩放失真，已删除该误导性文件并保留正常真实视口截图；该工具侧缩略伪影不作为页面缺陷或验收证据。

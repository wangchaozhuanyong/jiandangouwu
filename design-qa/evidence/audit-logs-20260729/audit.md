# 审计日志工作台运行与浏览器验收

日期：2026-07-29  
范围：正式管理后台 `/admin/logs`、`GET /v1/admin/audit?page=1&pageSize=100`  
证据等级：本地 API/MySQL/Valkey 运行探测、自动测试、生产构建、本地浏览器验收

## 真实性与数据边界

- 接口继续要求 `audit.read`；未登录直接请求返回 `401`。
- 使用一次性最小权限 QA 管理员登录后，接口返回 `200`，分页元数据为 `page=1`、`pageSize=100`。
- 运行响应中的事件字段只有 `action`、`actor`、`createdAt`、`id`、`reason`、`requestId`、`result`、`targetId`、`targetType`；操作者字段只有 `displayName`、`email`。
- 运行响应确认 `actorId`、`beforeData`、`afterData`、`ipHash` 均不存在。前端类型不再被当作安全边界，服务端 Prisma 查询使用显式 `select`。
- 页面明确显示已加载窗口与数据库总数；最近 24 小时和拒绝/失败统计只针对已加载窗口。当前没有完整历史翻页、服务器筛选、差异查看、导出或正式保留策略。
- QA 证据只使用 `invalid.example` 临时账号；原有管理员身份和历史操作者信息没有进入保存的截图。

## 自动检查

- `npm run check`：通过。
- 平台回归：45/45。
- 管理后台模型测试：41/41。
- API 测试：69/69。
- 客户端测试：6/6。
- 共享契约、API、客户端、管理后台与 CDK 类型检查通过；遗留 Vite/Sites 与全部主平台生产构建通过。
- 专项 API 单测验证审计查询没有 `include` 整行返回，也没有选择前后快照或 IP 哈希。

## 浏览器验收

### 1280 × 800，英文

- 页面只有一个 H1。
- 页面级横向溢出不大于 0。
- 审计表容器 `clientWidth=948`、`scrollWidth=1740`、`overflow-x=auto`，宽表只在自身容器滚动。
- 五个筛选控件均为 44px 高；页面所有可见按钮均不小于 44 × 44px。
- 关键词筛选、结果筛选、重置、真实刷新、无结果状态和详情弹窗均已操作验证。
- 详情弹窗显示九项白名单事实；关闭后焦点返回原详情按钮。

### 390 × 844，中文

- 页面只有一个 H1；标题、44px 菜单按钮和语言切换互不重叠。
- 页面级横向溢出不大于 0。
- 摘要卡与筛选均变为一列。
- 审计表容器 `clientWidth=349`、`scrollWidth=1740`，首列与详情列可在表格内部滚动时保持可用。
- 所有可见按钮均不小于 44 × 44px。

### 320 × 800，英文

- 页面只有一个 H1；换行后的 `Logs & monitoring` 与菜单、语言切换无重叠。
- 页面级横向溢出为 0。
- 摘要、筛选和两个筛选操作均为单列；操作按钮为 255 × 44px。
- 审计表容器 `clientWidth=279`、`scrollWidth=1740`。
- 首次详情验收发现 `.admin-content` 的路由入场动画使用 `fill-mode: both`，结束后残留 `transform`，使固定弹窗相对整页而非视口定位。移除残留 fill mode 后，计算样式为 `transform: none`。
- 修复后详情弹窗位于视口 `top=43.9`、`bottom=756.1`、`left=10`、`right=310`，完整处于视口内；详情内容横向溢出为 0，关闭按钮为 44 × 44px。

### 控制台

- 浏览器日志只有 Vite debug 与 React 开发提示。
- `warning=0`，`error=0`。

## 证据文件

- `01-desktop-en.png`：1280px 英文桌面、真实分页范围、筛选与表格内部横向滚动。
- `02-mobile-390-zh-top.png`：390px 中文顶部、摘要与筛选单列布局。
- `03-mobile-390-zh-table.png`：390px 中文表格区域与内部滚动。
- `04-mobile-320-en-top.png`：320px 英文标题换行、摘要与筛选布局。
- `05-mobile-320-en-detail.png`：320px 修复后的完整视口内详情弹窗。

## 清理

- 浏览器已退出一次性 QA 账号、恢复默认视口并关闭全部标签。
- 一次性 QA 用户与角色复核为 0；临时权限关联和以该用户为操作者的审计记录已删除。
- `/tmp/cloudbridge-audit-qa-credentials.json` 与临时 QA 脚本已删除。
- 未提交、未推送、未部署，未修改生产环境。
